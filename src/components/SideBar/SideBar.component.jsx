import { Layout } from 'antd';

// local imports
const { Sider } = Layout;

import './SideBar.style.scss';

const SideBar = () => {
  return <Sider className='c-sidebar'>SideBar</Sider>;
};

export default SideBar;
